# Local build and runtime artifacts
*.user
*.suo
bin/
obj/
*.log

# Keep packaged releases under outputs, but never commit local credentials/config.
config.json
credentials.json

# 分享包（由 share/使用说明.txt + outputs/CampusFlow.exe 打包而成，可随时重建）
share/*.zip
